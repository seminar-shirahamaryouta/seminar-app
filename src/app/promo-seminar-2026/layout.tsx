import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "プロモートビジネスセミナー入門編｜AIに代替されない働き方",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
